<%# 
    passing data to partial view
    each keyword added here becomes a local variable
    and each value will be the value of that local variable

    the underscore is not needed for form here
%>
<%= render 'form', page: @page %>